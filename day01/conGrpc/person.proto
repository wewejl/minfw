syntax="proto3";

package conGrpc;

service Person{
//    rpc
    rpc GetPerson(Request) returns(Response);
}

message Request {
    string name =1;
    int32 age = 2;
}

message Response {
    int32 age =2;
}